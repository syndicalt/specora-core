apiVersion: specora.dev/v1
kind: Workflow
metadata:
  name: user_lifecycle
  domain: todo_list
  description: The User entity has a single state with no transitions.
requires: []
spec:
  initial: none
  states:
    none:
      label: None State
      category: closed
      terminal: true
  transitions:
    none: []
